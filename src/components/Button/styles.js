import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button:{
        height: 48,
        width: 175,
        borderRadius: 10,
        borderWidth: 1,
        justifyContent:"center",
        marginBottom: 50
    },
    textButton:{
        color: "#FFFFFF",
        textAlign: "center",
        fontWeight: "bold",
        fontSize: 25
    }
})